# pnpm configuration for Nx monorepo
# See: https://pnpm.io/npmrc

# Hoist all dependencies to root node_modules for compatibility
# This helps with packages that don't properly declare peer dependencies
shamefully-hoist=true

# Automatically install peer dependencies
auto-install-peers=true

# Use strict peer dependency resolution (warn but don't fail)
strict-peer-dependencies=false
