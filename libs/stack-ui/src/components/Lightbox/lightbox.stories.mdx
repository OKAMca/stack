{/* lightbox.stories.mdx */}

import { Canvas, Meta, Story } from '@storybook/addon-docs'
import Lightbox from './'
import Image from 'next/image'

<Meta
  title="BASE COMPONENTS/Lightbox"
  component={Lightbox}
  args={{
    label: 'Open Dialog',
    thumbnailContent: <Image src={''} width={570} height={378} />,
  }}
/>

export const Template = (args) => (
  <Lightbox {...args}>
    <Image src={''} width={640} height={805} alt="Lightbox preview" />
  </Lightbox>
)

# Lightbox

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>
