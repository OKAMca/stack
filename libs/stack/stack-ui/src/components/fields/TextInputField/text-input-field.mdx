import { Canvas, Meta, Story, ArgTypes } from '@storybook/blocks'
import TextInputField from './index'
import * as TextInputFieldStories from './text-input-field.stories';

<Meta of={TextInputFieldStories} />

export const Template = (args) => {
  const { backgroundColor, ...rest } = args
  return (
    <div className={`flex flex-col m-auto justify-center items-center`}>
      <TextInputField {...rest} />
    </div>
  )
}

# Text Input Field

## Props

<ArgTypes exclude={[
  'fieldRef',
  'required',
  'disabled',
  'isError',
]} />

## Showcase

### Default

<Canvas of={TextInputFieldStories.Default} />

### Disabled

<Canvas of={TextInputFieldStories.Disabled} />

### Error

<Canvas of={TextInputFieldStories.Error} />

### Error with icon

<Canvas of={TextInputFieldStories.ErrorWithIcon} />
