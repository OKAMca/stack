import { Canvas, Meta, Story, ArgTypes } from '@storybook/blocks'
import DatePicker from './'
import DateRangePicker from './DateRangePicker'
import { OverlayContainer } from '@react-aria/overlays'
import * as DatePickerStories from './date-picker.stories';

<Meta of={DatePickerStories} />

# Date Picker

Date Picker component.

Calculations for this component are done with the [`@internationalized/date`](https://react-spectrum.adobe.com/internationalized/date/index.html) library.

## Props

<ArgTypes of={DatePickerStories.Default} />

Many date picker props are shared the the `Calendar` component. For more information, see the [Calendar documentation](?path=/docs/base-components-calendar--docs) for more information.

## Showcase

### Style variations

#### Default

<Canvas of={DatePickerStories.Default} />

#### Date field and icon

<Canvas of={DatePickerStories.DateFieldAndIcon} />

#### Only icon

<Canvas of={DatePickerStories.OnlyIcon} />

### Modified behaviors

#### Unavailable dates

In this example, unavailable dates will be rendered as disabled, making the user unable to select them. However, they are still technically selectable via the date field.

If the user attempts to select an unavailable date via the date field, the `isInvalid` state will be applied to the whole component as a token.

<Canvas of={DatePickerStories.UnavailableDates} />

#### Controlled

<Canvas of={DatePickerStories.Controlled} />
