{/* Checkbox.stories.mdx */}

import { Canvas, Meta, Story } from '@storybook/blocks'
import Checkbox from './index'
import Check from '../../icons/Check'
import Button from '../../Button'
import { useState } from 'react'
import * as CheckboxStories from './Checkbox.stories';

<Meta of={CheckboxStories} />

export const Template = (args) => <Checkbox {...args} />

export const TemplateWithButton = (args) => {
  const [checked, setChecked] = useState(args?.defaultSelected ?? false)
  const { onChange, ...restOfArgs } = args
  return (
    <div className={`flex gap-4 items-center`}>
      <Checkbox
        isSelected={checked}
        onChange={(isSelected) => {
          setChecked(!checked)
          onChange(isSelected)
        }}
        {...restOfArgs}
      />
      <Button handlePress={() => setChecked(!checked)}>Click to check</Button>
    </div>
  )
}

# Checkbox

Checkbox component.

## Default

<Canvas>
  <Story of={CheckboxStories.Default} />
</Canvas>

## Color variant

<Canvas>
  <Story of={CheckboxStories.Gray} />
</Canvas>

## With icon

<Canvas>
  <Story of={CheckboxStories.WithIcon} />
</Canvas>

## Disabled

<Canvas>
  <Story of={CheckboxStories.Disabled} />
</Canvas>

## Error

<Canvas>
  <Story of={CheckboxStories.Error} />
</Canvas>

## Check from other component

<Canvas>
  <Story of={CheckboxStories.CheckFromOtherComponent} />
</Canvas>
