import type { TBlockSerializerConfigComponent } from '@okam/directus-block'
import <%= componentName %> from '.'

const <%= variableName %>Config: TBlockSerializerConfigComponent = {
  <%= snakeName %>: {
    default: (props) => <<%= componentName %> {...props} />,
  },
}

export default <%= variableName %>Config
